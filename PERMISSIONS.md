# PERMISSIONS — dsh-asciinema 权限与失败边界声明

本文件供 DSH STORE 自动审查与人工复核使用，如实描述插件在运行时
做什么、不做什么，以及失败边界。

## 运行时行为
- **目的**：订阅 `session/event` 中 `tool/result` 输出，录制为 asciinema v2（`.cast`）；内嵌离线播放器回放 + HTML 导出。
- **读取**：会话事件流内 `tool/result` 文本输出（经宿主事件总线，不直读文件系统）。
- **写入**：录制产物 `.cast` 文件（默认工作区 `.casts/` 目录）；HTML 导出路径（工具参数指定）。
- **命令执行**：无子进程命令面——录制/回放/导出全部为包内 Node 代码（in-process）；不调用 shell、无外部二进制。
- **网络**：无。播放器数据内嵌，完全离线渲染，无外部请求。
- **凭据/密钥**：不读取、不写、不转发。
- **外部服务**：无。
- **全局资源**：不安装全局包、不改宿主配置。

## 依赖
| 依赖 | 用途 | 提供方 |
|---|---|---|
| Node.js ≥ 23.6 | 插件加载与工具执行 | DSH 宿主 |
| @deepseek-ai/cordis / dsh-fs / dsh-tools / schemastery | DSH 宿主提供的运行时服务（peer） | DSH 宿主 |

## 文件权限信号说明
- 运行时**不**执行 `chmod`/`chown`；`.cast` 与 HTML 以宿主默认权限创建。
- 不依赖可执行位（无子进程）；仓库文件均以 644 提交，无 setuid/setgid/sticky 信号。

## 失败边界（结构化，绝不静默）
- `maxEvents` / `maxBytes` 录制上限：超限截断并记录（不是无限增长）。
- 无外部依赖：即使播放器资源缺失也给出结构化错误，不静默产出坏 HTML。
- 录制器与会话生命周期绑定；同会话重名冲突有明确语义（take/dropSession）。
